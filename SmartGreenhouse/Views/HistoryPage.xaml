<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartGreenhouse.Views.HistoryPage"
             Title="History">

    <VerticalStackLayout Padding="12" Spacing="10">
        <Label Text="Recent Readings" FontSize="18" />
        <CollectionView ItemsSource="{Binding Readings}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="8" Margin="4">
                        <VerticalStackLayout>
                            <Label Text="{Binding Timestamp, StringFormat='{0:yyyy-MM-dd HH:mm:ss}'}" FontSize="12" />
                            <Label Text="{Binding TemperatureC, StringFormat='Temp: {0} °C'}" />
                            <Label Text="{Binding Humidity, StringFormat='Humidity: {0} %'}" />
                            <Label Text="{Binding SoilMoisture, StringFormat='Soil: {0} %'}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
